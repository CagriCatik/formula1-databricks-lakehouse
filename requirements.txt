# Static documentation site generator (https://zensical.org).
# Install into a virtual environment:
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows
#   source .venv/bin/activate     # macOS / Linux
#   pip install -r requirements.txt
zensical==0.0.44
PyYAML==6.0.2
